{% extends 'base.html.twig' %}

{% block title %}Bookcase{% endblock %}

{% block body %}
    <h1>Bookcase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bookcase.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bookcase.description }}</td>
            </tr>
            <tr>
                <th>Released</th>
                <td>{{ bookcase.released ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bookcase_index') }}">back to list</a>

    <a href="{{ path('app_bookcase_edit', {'id': bookcase.id}) }}">edit</a>

    {{ include('bookcase/_delete_form.html.twig') }}
{% endblock %}
